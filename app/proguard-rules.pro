# Add project specific ProGuard rules here.
# By default, the flags in this file are appended to flags specified
# in the Android SDK default ProGuard files.

# Keep ProGuard/R8 rules for Jetpack Compose and application code if necessary.
